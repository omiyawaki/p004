1) Key Narrative Claims
- The deck shifts to a global framing: how hot-day (`T^95`) events co-vary with soil moisture, latent heat, precipitation, and temperature across space and lead/lag times.
- Annual maps suggest strong positive land correlation between hot-tail latent-heat change and hot-tail soil-moisture change, while hot-tail temperature change is broadly anticorrelated with hot-tail soil-moisture change.
- For July composites around `T^95` days, anomalies are temporally structured: strongest signals cluster near event day (`-1 day`, `0 day`, `+1 day`) and weaken toward `+/-1 month`.
- The implied mechanism is moisture limitation around hot events: negative `SM`/`LH`/`P` anomalies co-occur with positive `T` anomalies over many land regions.

2) Figure-Level Highlights
- Slide 2: paired global maps show `R(ΔδLH^95, ΔδSM^95)` mostly positive over land and `R(ΔδT_{2m}^95, ΔδSM^95)` mostly negative, with regional exceptions.
- Slides 3-5: annual and monthly `ΔSM^95`/`Δ\overline{SM}`/`ΔδSM^95` maps indicate widespread drying in upper-percentile soil moisture over major land areas, with localized wetting pockets.
- Slides 6-9 (`MMM HISTORICAL July`): lead/lag composites for `T`, `LH`, `SM`, and `P` on `T^95` days show coherent event-centered anomalies; day-scale responses are stronger than month-scale responses.
- Slides 10-13 (`MMM SSP370-HISTORICAL July`): analogous anomaly and delta-anomaly composites preserve similar structure under forcing, with robust land hot-event signatures.
- Slide 14: difference maps (`[+1 month]-[-1 month]`, `[+2 weeks]-[-2 weeks]`, `[+1 week]-[-1 week]`, `[+1 day]-[-1 day]`) emphasize strongest asymmetry at shorter lags.

3) Reliability/Artifact Flags
- `pdftotext` extraction is effectively empty for both slide and notes PDFs (only title text), so machine-readable text evidence is minimal.
- The `meeting_notes` export appears to be reduced slide thumbnails without a readable speaker-notes pane; there is no usable presenter-note content to validate claims.
- Because presenter notes are absent, any detailed verbal interpretation beyond visible labels/colorbars would be high hallucination risk and should be avoided.
- Repeated layout/title templates (`MMM HISTORICAL July`, `MMM SSP370-HISTORICAL July`) indicate structured batch plotting; repetition should not be treated as independent confirmation.
- File metadata shows a Feb 28, 2026 Quartz re-export of May 10, 2023 material; provenance is archival, not original capture.

4) Paper-Outline Implications
- Position this meeting as a global, event-centric bridge from percentile-change diagnostics to process interpretation via hot-day lead/lag composites.
- Use a subsection flow: (i) global correlation framing, (ii) annual/monthly hot-tail soil-moisture structure, (iii) historical July event composites, (iv) forced-climate analogs and lag-asymmetry tests.
- Keep claims conservative: figures strongly support coherent co-variability patterns, but causal attribution remains inferential without additional process decomposition/statistical testing.
- Add a methods note that speaker-note text was unavailable in the archive, so synthesis is based on plotted fields/labels only.
