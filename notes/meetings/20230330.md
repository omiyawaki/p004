1) Key Narrative Claims
- Core framing: hot-extreme behavior is analyzed via coupled thermodynamic constraints, especially relationships among near-surface temperature metrics (`T2m`, `Tq=0`, `Tq=qs`) and free-tropospheric temperature (`T500`).
- Historical-state diagnostics suggest strong seasonality in percentile temperatures (mean/95th/99th) and a constrained joint `T500`-surface state envelope that sits below a theoretical upper bound.
- Future-minus-historical (`MMM SSP370-HISTORICAL`) maps imply that the 95th-percentile surface temperature response (`ΔδT2m95`/related metrics) is spatially heterogeneous: strong NH high-latitude cooling/weakening signals in some months, with land tropical/subtropical enhancements.
- Hydroclimate figures indicate strong seasonality in mean precipitation and `P95`, with projected `δP95` changes that are regionally dipolar (enhancements in some storm-track/tropical bands and reductions in others).
- Two month-by-month line plots of land `R^2(ΔδT95,δP95)` show a pronounced annual cycle: weakest coupling in boreal summer and stronger coupling in boreal cold seasons.

2) Figure-Level Highlights
- Slide 2-3: Seasonal cycles at `[+13.7,+106.2]` show `T2m` mean/95th/99th peaking in boreal spring; percentile spread persists year-round. `Tq=0` is comparatively flat seasonally, while `Tq=qs` has a strong annual cycle.
- Slide 4-5: Reanalysis/obs density contours in (`T500`, `Ts+q/cp`) space track below a dashed theoretical limit; CESM2 regional scatter examples show regime dependence (one region with tight low-slope cloud, another with broader positive slope and larger spread).
- Slide 7,10,11,15,19: Monthly global maps summarize `ΔδT2m95`, `P95`, `δP95`, `ΔT̄2m`, and `δLH95`; together they indicate coherent seasonal structure rather than uniform change.
- Slide 17: Land `R^2(ΔδT95,δP95)` minima occur around May-August and maxima in Jan-Feb plus late autumn/winter, suggesting seasonally varying explanatory power of hydroclimate for hot-extreme changes.
- Slides 9,12-14,16,18: Composite/cropped comparison panels juxtapose thermal and hydroclimate fields, reinforcing a joint-attribution story but with limited labeling clarity.

3) Reliability/Artifact Flags
- `pdftotext` extraction from both PDFs returned essentially no usable body text (only title), so all substantive content here comes from visual inspection of figures.
- The `20230330 meeting_notes.pdf` file appears to be a reduced-size slide export, not a true presenter-notes document (no readable notes pane/text); treat any assumption of spoken notes as unsupported.
- PDF metadata `CreationDate`/`ModDate` are February 28, 2026 (not March 30, 2023), indicating a later re-export/backfill. Content may still reflect the 2023 meeting, but provenance should be treated cautiously.
- Several late-deck panels are visibly cropped/truncated (titles/colorbar labels partly cut), increasing risk of variable misidentification (`ΔT50` vs `ΔδT95` variants).
- Because notes text is unavailable, narrative claims that require causal interpretation (beyond visual co-variation) should be considered tentative.

4) Paper-Outline Implications
- Structure a results arc around a constrained-thermodynamic baseline (`T500`-surface envelope) followed by regime/region-specific departures in model space.
- Separate thermal-extreme metrics (`ΔT̄2m`, `ΔT95`, `ΔδT95`) from hydroclimate metrics (`P̄`, `P95`, `δP95`, `δLH95`) before presenting coupled diagnostics; this mirrors the deck logic and reduces conflation.
- Use seasonality as a primary organizing axis (monthly maps + monthly `R^2`), since coupling strength clearly varies by month.
- Add a methods/provenance note on figure reconstruction limits for this meeting (missing machine-readable text and missing true presenter notes) and avoid over-claiming mechanistic causality from this deck alone.
